import { customerSaysContent as content } from '../content/site'; import { SectionHeading } from './shared';
export function CustomerStories(){return <section id={content.sectionId} className="section"><div className="container"><SectionHeading eyebrow={content.subheading}>{content.heading}</SectionHeading><div className="stories-grid">{content.stories.map((s,i)=><article className="story-card" key={s.title}><div className={`media-placeholder media-${i+1}`}><button aria-label={`Play ${s.title} customer story`}>▶</button><span>{s.durationLabel}</span></div><div><h3>{s.title}</h3><p>{s.location}</p></div></article>)}</div><a className="text-link" href={content.cta.target}>{content.cta.label} →</a></div></section>}
