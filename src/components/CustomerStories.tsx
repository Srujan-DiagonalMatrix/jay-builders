import { customerSaysContent as content } from '../content/site'; import { getImageAsset } from '../content/image-manifest'; import { SectionHeading } from './shared'; import { ResponsiveImage } from './ResponsiveImage';
export function CustomerStories(){return <section id={content.sectionId} className="section"><div className="container"><SectionHeading eyebrow={content.subheading}>{content.heading}</SectionHeading><div className="stories-grid">{content.stories.map((s,i)=><article className="story-card" key={s.title}><div className="media-placeholder"><ResponsiveImage asset={getImageAsset(`CustomerSays-story-0${i+1}`)} sizes="(max-width: 575px) 100vw, (max-width: 991px) 50vw, 33vw"/><button aria-label={`Play ${s.title} customer story`}>▶</button><span>{s.durationLabel}</span></div><div><h3>{s.title}</h3><p>{s.location}</p></div></article>)}</div><a className="text-link" href={content.cta.target}>{content.cta.label} →</a></div></section>}
