import { stories } from '../content/site'; import { SectionHeading } from './shared';
export function CustomerStories(){return <section id="customer-stories" className="section"><div className="container"><SectionHeading eyebrow="Real customers. Real projects. Real results.">See what our customers say</SectionHeading><div className="stories-grid">{stories.map((s,i)=><article className="story-card" key={s.title}><div className={`media-placeholder media-${i+1}`}><button aria-label={`Play ${s.title} customer story`}>▶</button><span>{s.duration}</span></div><div><h3>{s.title}</h3><p>{s.location}</p></div></article>)}</div><a className="text-link" href="#recommendations">Watch More Customer Stories →</a></div></section>}
