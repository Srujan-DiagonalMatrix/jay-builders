import { projects } from '../content/site'; import { SectionHeading } from './shared';
export function ProjectGallery(){return <section id="our-work" className="section section--soft"><div className="container"><SectionHeading>Our work speaks for itself</SectionHeading><div className="projects-grid">{projects.map((p,i)=><article className="project-card" key={p[0]}><div className={`project-image project-image--${i+1}`} aria-hidden="true"><span>Before</span><span>After</span></div><h3>{p[0]}</h3><p>{p[1]} <b>{p[2]}</b></p></article>)}</div><a className="button button--navy centered-button" href="#contact">View More Projects</a></div></section>}
