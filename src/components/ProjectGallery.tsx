import { ourWorkContent as content } from '../content/site'; import { SectionHeading } from './shared';
export function ProjectGallery(){return <section id={content.sectionId} className="section section--soft"><div className="container"><SectionHeading>{content.heading}</SectionHeading><div className="projects-grid">{content.projects.map((p,i)=><article className="project-card" key={p.title}><div className={`project-image project-image--${i+1}`} aria-hidden="true"/><h3>{p.title}</h3><p>{p.location} <b>{p.duration}</b></p></article>)}</div><a className="button button--navy centered-button" href={content.cta.target}>{content.cta.label}</a></div></section>}
