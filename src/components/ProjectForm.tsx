import type { FormEvent } from 'react'; import { projectFormContent as content } from '../content/site'; import { SectionHeading } from './shared';
export function ProjectForm(){function submit(e:FormEvent){e.preventDefault()} return <section id={content.sectionId} className="subsection form-section"><SectionHeading>{content.heading}</SectionHeading><p>{content.intro}</p><form onSubmit={submit}><div className="form-grid">{content.fields.map(f=><div className={f.type==='textarea'||f.type==='file'?'field field--wide':'field'} key={f.name}><label htmlFor={f.name}>{f.label}{f.required&&<span aria-hidden="true"> *</span>}</label>{f.type==='textarea'?<textarea id={f.name} name={f.name} required={f.required} rows={5}/>:f.type==='select'?<select id={f.name} name={f.name} required={f.required} defaultValue=""><option value="" aria-label="No selection"/>{f.options?.map(o=><option key={o}>{o}</option>)}</select>:<input id={f.name} name={f.name} type={f.type} required={f.required} accept={f.accept?.join(',')} multiple={f.multiple}/>}</div>)}</div><button className="button button--gold" type="submit">{content.cta.label}</button><p className="privacy">{content.privacyNote}</p></form></section>}
