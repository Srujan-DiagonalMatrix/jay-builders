import { projectSpotlightContent as content } from '../content/site'; import { getImageAsset } from '../content/image-manifest'; import { SectionHeading } from './shared'; import { ResponsiveImage } from './ResponsiveImage';
export function ProjectSpotlight(){return <section id={content.sectionId} className="section"><div className="container spotlight-grid"><div className="spotlight-copy"><SectionHeading eyebrow={content.eyebrow}>{content.heading[0]}<br/>{content.heading[1]}</SectionHeading><p>{content.body}</p><ul>{content.scope.map(item=><li key={item}>{item}</li>)}</ul><a className="button button--gold" href={content.cta.target}>{content.cta.label}</a></div><blockquote className="spotlight-quote">“{content.testimonial.quote}”<cite>— {content.testimonial.attribution}</cite></blockquote><div className="spotlight-gallery"><ResponsiveImage asset={getImageAsset('OutdatedProp-spotlight')} sizes="(max-width: 575px) 100vw, 30vw"/></div></div></section>}
