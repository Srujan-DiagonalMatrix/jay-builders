import { services } from '../content/site'; import { SectionHeading } from './shared';
export function ServicesGrid(){return <section id="services" className="section"><div className="container"><SectionHeading>Complete building services under one roof</SectionHeading><div className="services-grid">{services.map(s=><article className="service-card" key={s[0]}><img src={`/assets/icons/${s[2]}.svg`} alt="" width="40" height="40"/><h3>{s[0]}</h3><p>{s[1]}</p></article>)}<aside className="guidance-card"><h3>Not sure which service you need?</h3><p>Tell us about your project and we’ll guide you.</p><a href="#contact">Speak to JAY Builders →</a></aside></div></div></section>}
