import { recommendationsContent as content } from '../content/site'; import { SectionHeading } from './shared';
export function Recommendations(){return <section id={content.sectionId} className="subsection"><SectionHeading>{content.heading}</SectionHeading><div className="reviews-grid">{content.reviews.map(r=><article className="review-card" key={r.title}><p className="stars" aria-label={`${r.rating} out of 5 stars`}>{'★'.repeat(r.rating)}</p><h3>{r.title}</h3><p>“{r.quote}”</p><footer><strong>{r.attribution}</strong><span>{r.platform}</span></footer></article>)}</div></section>}
