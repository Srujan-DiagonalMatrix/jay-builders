import { headerHeroContent as content } from '../content/site'; import { getImageAsset } from '../content/image-manifest'; import { ResponsiveImage } from './ResponsiveImage';
export function Hero(){return <section id={content.sectionId} className="hero" aria-labelledby="hero-title"><ResponsiveImage asset={getImageAsset('Header-hero')} sizes="100vw" priority className="hero-media"/><div className="container hero-inner"><div className="hero-copy"><h1 id="hero-title">{content.headline[0]}<br/><strong>{content.headline[1]}</strong></h1><p className="hero-subhead">{content.subhead[0]}<br/>{content.subhead[1]}</p><ul className="trust-list">{content.trustPoints.map(point=><li key={point}>{point}</li>)}</ul><div className="hero-actions"><a className="button button--gold" href={content.primaryCta.target}>{content.primaryCta.label}</a><a className="button button--outline" href={content.secondaryCta.target}>{content.secondaryCta.label}</a></div><blockquote>“{content.featuredCustomerQuote.quote}”<cite>— {content.featuredCustomerQuote.attribution}</cite></blockquote></div></div></section>}
