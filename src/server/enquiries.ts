import { randomUUID } from 'node:crypto';
import { EnquiryValues, fileExtension, validateEnquiry, validateFiles } from '../components/ProjectForm/schema';
export const MAX_PAYLOAD_SIZE=8*20*1024*1024+64*1024; type RateRecord={count:number;reset:number};
export type EnquiryResult={status:number;body:{message:string;requestId:string;errors?:Record<string,string>}};
export class EnquiryService{private requests=new Map<string,RateRecord>();constructor(private now=()=>Date.now(),private save:(data:EnquiryValues,files:Array<{file:File;storageName:string}>)=>Promise<void>=async()=>{}){}
 async handle(form:FormData,ip='unknown',requestId:string=randomUUID()):Promise<EnquiryResult>{const current=this.now(),record=this.requests.get(ip);if(record&&record.reset>current&&record.count>=5)return{status:429,body:{message:'Please wait before trying again.',requestId}};this.requests.set(ip,!record||record.reset<=current?{count:1,reset:current+60_000}:{...record,count:record.count+1});const value=(key:string)=>{const entry=form.get(key);return typeof entry==='string'?entry:''};const data:EnquiryValues={fullName:value('fullName'),email:value('email'),phone:value('phone'),postcode:value('postcode'),workType:value('workType'),startDate:value('startDate'),budget:value('budget'),contactMethod:value('contactMethod'),description:value('description'),website:value('website')};
  if(data.website)return{status:200,body:{message:'Enquiry received.',requestId}};const files=form.getAll('files').filter((entry):entry is File=>typeof entry!=='string'&&entry.size>0);const errors={...validateEnquiry(data)};const fileError=validateFiles(files);if(fileError)errors.files=fileError;if(Object.keys(errors).length)return{status:400,body:{message:'Please check the form and try again.',requestId,errors}};const safeFiles=files.map(file=>({file,storageName:`${randomUUID()}${fileExtension(file.name)}`}));try{await this.save(data,safeFiles);return{status:201,body:{message:'Enquiry received.',requestId}}}catch{return{status:500,body:{message:'We could not process your enquiry. Please try again later.',requestId}}}}
}
